from pydub import AudioSegment

def encryptAudio(audio, key):
	encryptedAudio = []
	# chunkSize is the length of each element
	chunkSize = 1000
	keyLen = len(key)
	


def decryptAudio(audioFile, key):
	x=""

if __name__=="__main__":
	audio = AudioSegment.from_mp3("audioFile.mp3")
	key = "game"


